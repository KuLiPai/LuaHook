<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 17+：监听端口供局域网设备访问需要单独的运行时权限。 -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:description="@string/xposeddescription"
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LuaHook"
        tools:targetApi="35">
        <activity android:name=".ui.project.config.ProjectConfigActivity"
            android:exported="false" />
        <activity android:name=".ui.project.editor.ProjectEditorActivity"
            android:exported="false" />
        <activity android:name=".ui.script.selector.ScopeSelectorActivity"
            android:exported="false" />
        <activity android:name=".ui.project.create.CreateProjectActivity"
            android:exported="false" />
        <activity
            android:name=".ui.error.ErrorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.script.manager.MultiScriptActivity"
            android:exported="false" />
        <activity
            android:name=".ui.script.setting.ScriptSetActivity"
            android:exported="true" />
        <activity
            android:name=".ui.about.AboutActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.script.selector.SelectApps"
            android:exported="false" />
        <activity
            android:name=".ui.script.editor.app.AppsEdit"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".ui.manual.Manual"
            android:exported="false" />
        <activity
            android:name=".ui.logcat.LogCatActivity"
            android:exported="false" />
        <activity
            android:name=".ui.script.editor.global.EditActivity"
            android:windowSoftInputMode="adjustResize"

            android:exported="false" />
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- xposedminversion=53。不要抬到 100/102，否则 API 93、100 的管理器会提示为较新版本设计。入口是 assets/xposed_init 的 MainHook。 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposeddescription" />
        <meta-data
            android:name="xposedminversion"
            android:value="53" />

        <provider
            android:name="io.github.libxposed.service.XposedProvider"
            android:authorities="${applicationId}.XposedService"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
        <service
            android:name=".mcp.McpForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <receiver
            android:name=".mcp.McpBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.kulipai.luahook.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
